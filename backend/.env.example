# Root / docker-compose env (rename to .env for local)
POSTGRES_PASSWORD=postgrespass
POSTGRES_USER=shearbite
POSTGRES_DB=shearbite_dev
REDIS_PASSWORD=redispass

# Traefik
TRAEFIK_HOST=traefik.local

# JWT keys (dev — replace with secure keys in prod)
JWT_SECRET=changeme_dev_secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRES_MINUTES=15
REFRESH_TOKEN_EXPIRES_DAYS=30

# Cookie domain (for dev might be localhost)
COOKIE_DOMAIN=localhost

